protocol bgp franceix_13285 from UPSTREAM {
    neighbor 37.49.237.3 as 13285;
    description "france-ix / TalkTalk";
        
    import limit 200;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(13285,37.49.237.3,"peering",210,209) then reject;
        accept;
    };
}
