protocol bgp lucix_13335 from UPSTREAM {
    neighbor 188.93.170.100 as 13335;
    description "lu-cix / Cloudflare";
        
    import limit 500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(13335,188.93.170.100,"peering",301) then reject;
        accept;
    };
}
protocol bgp franceix_13335 from UPSTREAM {
    neighbor 37.49.237.49 as 13335;
    description "france-ix / Cloudflare";
        
    import limit 500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(13335,37.49.237.49,"peering",201) then reject;
        accept;
    };
}
