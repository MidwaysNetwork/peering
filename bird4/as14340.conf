protocol bgp franceix_14340 from UPSTREAM {
    neighbor 37.49.237.154 as 14340;
    description "france-ix / Salesforce.com";
        
    import limit 1000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(14340,37.49.237.154,"peering",201) then reject;
        accept;
    };
}
