protocol bgp franceix_14413 from UPSTREAM {
    neighbor 37.49.236.161 as 14413;
    description "france-ix / Linkedin";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(14413,37.49.236.161,"peering",210,209) then reject;
        accept;
    };
}
