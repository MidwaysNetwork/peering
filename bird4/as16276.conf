protocol bgp franceix_16276 from UPSTREAM {
    neighbor 37.49.236.144 as 16276;
    description "france-ix / OVH";
        
    import limit 1000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(16276,37.49.236.144,"peering",201,218) then reject;
        accept;
    };
}
