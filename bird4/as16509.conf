protocol bgp franceix_16509 from UPSTREAM {
    neighbor 37.49.236.118 as 16509;
    description "france-ix / Amazon.com";
        
    import limit 5000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(16509,37.49.236.118,"peering",201) then reject;
        accept;
    };
}
protocol bgp franceix_16509_2 from UPSTREAM {
    neighbor 37.49.237.166 as 16509;
    description "france-ix / Amazon.com";
        
    import limit 5000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(16509,37.49.237.166,"peering",201) then reject;
        accept;
    };
}
