protocol bgp franceix_19679 from UPSTREAM {
    neighbor 37.49.237.147 as 19679;
    description "france-ix / Dropbox";
        
    import limit 100;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(19679,37.49.237.147,"peering",200) then reject;
        accept;
    };
}
protocol bgp topix_19679 from UPSTREAM {
    neighbor 194.116.96.95 as 19679;
    description "top-ix: / Dropbox";
        
    import limit 100;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(19679,194.116.96.95,"peering",408) then reject;
        accept;
    };
}
