protocol bgp lucix_197692 from UPSTREAM {
    neighbor 188.93.170.31 as 197692;
    description "lu-cix / Conostix S.A.";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(197692,188.93.170.31,"peering",301) then reject;
        accept;
    };
}
protocol bgp lucix_197692_2 from UPSTREAM {
    neighbor 188.93.171.31 as 197692;
    description "lu-cix / Conostix S.A.";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(197692,188.93.171.31,"peering",301) then reject;
        accept;
    };
}
protocol bgp franceix_197692 from UPSTREAM {
    neighbor 37.49.236.133 as 197692;
    description "france-ix / Conostix S.A.";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(197692,37.49.236.133,"peering",205) then reject;
        accept;
    };
}
