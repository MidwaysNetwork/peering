protocol bgp franceix_198545 from UPSTREAM {
    neighbor 37.49.236.157 as 198545;
    description "france-ix / FLEX NETWORK";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(198545,37.49.236.157,"peering",201,218) then reject;
        accept;
    };
}
