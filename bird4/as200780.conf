protocol bgp franceix_200780 from UPSTREAM {
    neighbor 37.49.237.100 as 200780;
    description "france-ix / APPLIWAVE";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(200780,37.49.237.100,"peering",201) then reject;
        accept;
    };
}
