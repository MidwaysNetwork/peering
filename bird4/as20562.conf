protocol bgp lucix_20562 from UPSTREAM {
    neighbor 188.93.170.47 as 20562;
    description "lu-cix / Open Peering";
        
    import limit 5000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(20562,188.93.170.47,"peering",309) then reject;
        accept;
    };
}
protocol bgp franceix_20562 from UPSTREAM {
    neighbor 37.49.236.68 as 20562;
    description "france-ix / Open Peering";
        
    import limit 5000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(20562,37.49.236.68,"peering",200) then reject;
        accept;
    };
}
