protocol bgp lucix_20940 from UPSTREAM {
    neighbor 188.93.170.95 as 20940;
    description "lu-cix / Akamai Technologies";
        
    import limit 6500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(20940,188.93.170.95,"peering",311) then reject;
        accept;
    };
}
protocol bgp franceix_20940 from UPSTREAM {
    neighbor 37.49.236.168 as 20940;
    description "france-ix / Akamai Technologies";
        
    import limit 6500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(20940,37.49.236.168,"peering",201) then reject;
        accept;
    };
}
