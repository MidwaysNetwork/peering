protocol bgp franceix_2611 from UPSTREAM {
    neighbor 37.49.236.100 as 2611;
    description "france-ix / BELNET";
        
    import limit 55;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(2611,37.49.236.100,"peering",212) then reject;
        accept;
    };
}
