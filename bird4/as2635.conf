protocol bgp franceix_2635 from UPSTREAM {
    neighbor 37.49.237.157 as 2635;
    description "france-ix / Automattic";
        
    import limit 150;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(2635,37.49.237.157,"peering",200) then reject;
        accept;
    };
}
