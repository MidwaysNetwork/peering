protocol bgp lucix_29467 from UPSTREAM {
    neighbor 188.93.170.56 as 29467;
    description "lu-cix / LuxNetwork S.A.";
        
    import limit 500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(29467,188.93.170.56,"peering",211,208) then reject;
        accept;
    };
}
protocol bgp lucix_29467_2 from UPSTREAM {
    neighbor 188.93.171.56 as 29467;
    description "lu-cix / LuxNetwork S.A.";
        
    import limit 500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(29467,188.93.171.56,"peering",211,208) then reject;
        accept;
    };
}
protocol bgp franceix_29467 from UPSTREAM {
    neighbor 37.49.236.227 as 29467;
    description "france-ix / LuxNetwork S.A.";
        
    import limit 500;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(29467,37.49.236.227,"peering",200,219) then reject;
        accept;
    };
}
