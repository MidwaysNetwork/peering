protocol bgp franceix_36692 from UPSTREAM {
    neighbor 37.49.236.210 as 36692;
    description "france-ix / OpenDNS, Inc.";
        
    import limit 70;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(36692,37.49.236.210,"peering",200) then reject;
        accept;
    };
}
