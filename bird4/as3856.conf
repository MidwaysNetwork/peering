protocol bgp franceix_3856 from UPSTREAM {
    neighbor 37.49.236.91 as 3856;
    description "france-ix / Packet Clearing House";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(3856,37.49.236.91,"peering",200,219) then reject;
        accept;
    };
}
protocol bgp topix_3856 from UPSTREAM {
    neighbor 194.116.96.59 as 3856;
    description "top-ix: / Packet Clearing House";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(3856,194.116.96.59,"peering",219,200) then reject;
        accept;
    };
}
