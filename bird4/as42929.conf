protocol bgp franceix_42929 from UPSTREAM {
    neighbor 37.49.236.224 as 42929;
    description "france-ix / Artefact - Artewan";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(42929,37.49.236.224,"peering",201) then reject;
        accept;
    };
}
