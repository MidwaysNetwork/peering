protocol bgp franceix_54994 from UPSTREAM {
    neighbor 37.49.237.155 as 54994;
    description "france-ix / Quantil Networks";
        
    import limit 2000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(54994,37.49.237.155,"peering",202) then reject;
        accept;
    };
}
