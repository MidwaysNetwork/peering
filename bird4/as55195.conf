protocol bgp franceix_55195 from UPSTREAM {
    neighbor 37.49.237.64 as 55195;
    description "france-ix / Canadian Internet Registration Authority - DNS";
        
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(55195,37.49.237.64,"peering",207) then reject;
        accept;
    };
}
