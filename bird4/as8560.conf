protocol bgp franceix_8560 from UPSTREAM {
    neighbor 37.49.236.42 as 8560;
    description "france-ix / 1&1 Internet";
        
    import limit 5000;
    export filter {
        if ! check_export4() then reject;
        accept;
    };
    import filter {
        if ! check_import4(8560,37.49.236.42,"peering",201) then reject;
        accept;
    };
}
