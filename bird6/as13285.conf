protocol bgp franceix_13285  from UPSTREAM {
    neighbor 2001:7f8:54::1:3 as 13285;
    description "france-ix / TalkTalk";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(13285,2001:7f8:54::1:3,"peering",210,209) then reject;
        accept;
    };

}
