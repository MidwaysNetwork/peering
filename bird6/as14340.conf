protocol bgp franceix_14340  from UPSTREAM {
    neighbor 2001:7f8:54::1:154 as 14340;
    description "france-ix / Salesforce.com";
        
    import limit 100;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(14340,2001:7f8:54::1:154,"peering",201) then reject;
        accept;
    };

}
