protocol bgp franceix_16276  from UPSTREAM {
    neighbor 2001:7f8:54::144 as 16276;
    description "france-ix / OVH";
        
    import limit 200;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(16276,2001:7f8:54::144,"peering",201,218) then reject;
        accept;
    };

}
