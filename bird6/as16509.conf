protocol bgp franceix_16509  from UPSTREAM {
    neighbor 2001:7f8:54::118 as 16509;
    description "france-ix / Amazon.com";
        
    import limit 2000;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(16509,2001:7f8:54::118,"peering",201) then reject;
        accept;
    };

}
protocol bgp franceix_16509_2  from UPSTREAM {
    neighbor 2001:7f8:54::1:166 as 16509;
    description "france-ix / Amazon.com";
        
    import limit 2000;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(16509,2001:7f8:54::1:166,"peering",201) then reject;
        accept;
    };

}
