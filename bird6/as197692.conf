protocol bgp lucix_197692  from UPSTREAM {
    neighbor 2001:7f8:4c::3043:c:1 as 197692;
    description "lu-cix / Conostix S.A.";
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(197692,2001:7f8:4c::3043:c:1,"peering",211,208) then reject;
        accept;
    };

}
protocol bgp lucix_197692_2  from UPSTREAM {
    neighbor 2001:7f8:4c::3043:c:2 as 197692;
    description "lu-cix / Conostix S.A.";
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(197692,2001:7f8:4c::3043:c:2,"peering",211,208) then reject;
        accept;
    };

}
protocol bgp franceix_197692  from UPSTREAM {
    neighbor 2001:7f8:54::133 as 197692;
    description "france-ix / Conostix S.A.";
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(197692,2001:7f8:54::133,"peering",205,214) then reject;
        accept;
    };

}
