protocol bgp franceix_198545  from UPSTREAM {
    neighbor 2001:7f8:54::157 as 198545;
    description "france-ix / FLEX NETWORK";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(198545,2001:7f8:54::157,"peering",201) then reject;
        accept;
    };

}
