protocol bgp franceix_200780  from UPSTREAM {
    neighbor 2001:7f8:54::1:100 as 200780;
    description "france-ix / APPLIWAVE";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(200780,2001:7f8:54::1:100,"peering",200,219) then reject;
        accept;
    };

}
