protocol bgp lucix_20940  from UPSTREAM {
    neighbor 2001:7f8:4c::51cc:1 as 20940;
    description "lu-cix / Akamai Technologies";
        
    import limit 1500;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(20940,2001:7f8:4c::51cc:1,"peering",311) then reject;
        accept;
    };

}
protocol bgp franceix_20940  from UPSTREAM {
    neighbor 2001:7f8:54::168 as 20940;
    description "france-ix / Akamai Technologies";
        
    import limit 1500;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(20940,2001:7f8:54::168,"peering",201) then reject;
        accept;
    };

}
