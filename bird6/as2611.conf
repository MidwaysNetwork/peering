protocol bgp franceix_2611  from UPSTREAM {
    neighbor 2001:7f8:54::100 as 2611;
    description "france-ix / BELNET";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(2611,2001:7f8:54::100,"peering",213) then reject;
        accept;
    };

}
