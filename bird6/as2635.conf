protocol bgp franceix_2635  from UPSTREAM {
    neighbor 2001:7f8:54::1:157 as 2635;
    description "france-ix / Automattic";
        
    import limit 100;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(2635,2001:7f8:54::1:157,"peering",200) then reject;
        accept;
    };

}
