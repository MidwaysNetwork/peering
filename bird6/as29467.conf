protocol bgp lucix_29467  from UPSTREAM {
    neighbor 2001:7f8:4c::731b:1 as 29467;
    description "lu-cix / LuxNetwork S.A.";
        
    import limit 150;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(29467,2001:7f8:4c::731b:1,"peering",303) then reject;
        accept;
    };

}
protocol bgp lucix_29467_2  from UPSTREAM {
    neighbor 2001:7f8:4c::731b:2 as 29467;
    description "lu-cix / LuxNetwork S.A.";
        
    import limit 150;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(29467,2001:7f8:4c::731b:2,"peering",303) then reject;
        accept;
    };

}
protocol bgp franceix_29467  from UPSTREAM {
    neighbor 2001:7f8:54::227 as 29467;
    description "france-ix / LuxNetwork S.A.";
        
    import limit 150;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(29467,2001:7f8:54::227,"peering",200) then reject;
        accept;
    };

}
