protocol bgp franceix_32934  from UPSTREAM {
    neighbor 2001:7f8:54::174 as 32934;
    description "france-ix / Facebook Inc";
        
    import limit 100;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(32934,2001:7f8:54::174,"peering",217) then reject;
        accept;
    };

}
protocol bgp franceix_32934_2  from UPSTREAM {
    neighbor 2001:7f8:54::175 as 32934;
    description "france-ix / Facebook Inc";
        
    import limit 100;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(32934,2001:7f8:54::175,"peering",217) then reject;
        accept;
    };

}
protocol bgp topix_32934  from UPSTREAM {
    neighbor 2001:7f8:23:ffff::92 as 32934;
    description "top-ix: / Facebook Inc";
        
    import limit 100;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(32934,2001:7f8:23:ffff::92,"peering",407) then reject;
        accept;
    };

}
protocol bgp topix_32934_2  from UPSTREAM {
    neighbor 2001:7f8:23:ffff::93 as 32934;
    description "top-ix: / Facebook Inc";
        
    import limit 100;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(32934,2001:7f8:23:ffff::93,"peering",407) then reject;
        accept;
    };

}
