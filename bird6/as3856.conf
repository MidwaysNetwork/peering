protocol bgp franceix_3856  from UPSTREAM {
    neighbor 2001:7f8:54::91 as 3856;
    description "france-ix / Packet Clearing House";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(3856,2001:7f8:54::91,"peering",200,219) then reject;
        accept;
    };

}
protocol bgp topix_3856  from UPSTREAM {
    neighbor 2001:7f8:23:ffff::13 as 3856;
    description "top-ix: / Packet Clearing House";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(3856,2001:7f8:23:ffff::13,"peering",219,200) then reject;
        accept;
    };

}
