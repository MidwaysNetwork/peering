protocol bgp franceix_54994  from UPSTREAM {
    neighbor 2001:7f8:54::1:155 as 54994;
    description "france-ix / Quantil Networks";
        
    import limit 200;
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(54994,2001:7f8:54::1:155,"peering",201,218) then reject;
        accept;
    };

}
