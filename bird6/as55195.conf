protocol bgp franceix_55195  from UPSTREAM {
    neighbor 2001:7f8:54::1:64 as 55195;
    description "france-ix / Canadian Internet Registration Authority - DNS";
        
    export filter {
        if ! check_export6() then reject;
        accept;
    };
    import filter {
        if ! check_import6(55195,2001:7f8:54::1:64,"peering",207) then reject;
        accept;
    };

}
